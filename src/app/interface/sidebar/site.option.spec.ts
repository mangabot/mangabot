/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {SiteOption} from './site.option';

describe('Site', () => {
  it('should create an instance', () => {
    expect(new SiteOption()).toBeTruthy();
  });
});
